const CACHE='goodast-v4';const urls=['/The-goodast-tyre-fix/','/The-goodast-tyre-fix/index.html','/The-goodast-tyre-fix/business.html','/The-goodast-tyre-fix/banner.jpg','/The-goodast-tyre-fix/logo.jpg','/The-goodast-tyre-fix/price-list.jpg','/The-goodast-tyre-fix/manifest.json'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(urls)))});self.addEventListener('fetch',e=>{e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request)))});
